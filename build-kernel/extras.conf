# Custom kernel config additions for Lima
# These options are appended to the Kata kernel config

# ISO9660 support (required for cloud-init cidata mounting)
CONFIG_ISO9660_FS=y
CONFIG_JOLIET=y
CONFIG_ZISOFS=y
